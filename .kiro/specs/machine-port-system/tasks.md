# Implementation Plan

- [x] 1. PortMath — ポート回転計算の純粋関数
- [x] 1.1 テスト: ポートオフセットと方向の回転計算
  - 2x2機械での全4方向（N/E/S/W）オフセット回転が正しい座標を返すことを検証
  - 4回同方向に回転すると元のオフセットに戻ることを検証
  - ポート方向の回転が機械方向に正しく追従することを検証
  - 方向→単位ベクトル変換（N→上、E→右、S→下、W→左）の正確性を検証
  - 接続先ベルト位置の計算（ポート位置+方向ベクトル）の正確性を検証
  - _Requirements: 1.3, 1.4_

- [x] 1.2 実装: ポート回転計算の純粋関数群
  - グリッド座標での90°刻み回転公式を実装（正方形・非正方形対応）
  - ポート方向の加算回転を実装
  - 方向→単位ベクトル変換を実装（Godot Y軸下向き座標系）
  - ポートのワールド位置から接続先ベルト位置を計算する関数を実装
  - SceneTree非依存のRefCountedクラスとして作成
  - _Requirements: 1.3, 1.4_

- [x] 2. MachinePortConfig / MachinePortCatalog — ポート構成定義
- [x] 2.1 (P) テスト: ポート構成データの登録・取得
  - MVP3機械（採掘機・精錬機・納品箱）の create_default() でカタログが正しく構築されることを検証
  - 各機械タイプIDで正しいポート構成（入力ポート数・出力ポート数・オフセット・方向）が返ることを検証
  - 未登録の機械タイプIDでnullが返ることを検証
  - 非機械エンティティ（ベルト）がポート構成を持たないことを検証
  - _Requirements: 1.1, 1.2, 1.5_

- [x] 2.2 (P) 実装: ポート構成データとカタログ
  - ポート定義のデータ構造を作成（ローカルオフセット・ローカル方向）
  - 機械タイプごとのポート構成データクラスを作成（入力ポート配列・出力ポート配列・機械サイズ）
  - カタログクラスを作成（登録・取得・存在チェック）
  - create_default() でMVP3機械のポート構成を登録: 採掘機（出力1）、精錬機（入力1・出力1）、納品箱（入力1）
  - SceneTree非依存のRefCounted/Resourceクラスとして作成
  - _Requirements: 1.1, 1.2, 1.5_

- [x] 3. MachinePortGrid — ポート登録・接続管理
- [x] 3.1 テスト: 機械登録・解除とポートインスタンス生成
  - 機械登録時にポートが正しいワールド座標・方向で生成されることを検証（北向き）
  - 全4方向の回転で正しいワールド座標・方向に変換されることを検証（2x2機械）
  - 機械解除時に該当ポートが完全に削除されることを検証
  - 1x1機械（納品箱）と2x2機械（採掘機・精錬機）の両方で正しく動作することを検証
  - ポートバッファの初期状態が空であることを検証
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 6.3_

- [x] 3.2 実装: 機械の登録・解除
  - 機械配置時にポート構成を取得し、回転計算でワールド座標のポートインスタンスを生成するロジックを実装
  - エンティティID別のポート管理とワールド位置別のインデックスを実装
  - 機械撤去時のポート削除とバッファクリアを実装
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 6.3_

- [x] 3.3 テスト: ポート接続解決とdirty再構築
  - 出力ポートに隣接ベルトが存在する場合に接続が成立することを検証
  - 入力ポートに隣接ベルトが存在し方向互換の場合に接続が成立することを検証
  - 隣接にベルトが存在しない場合に接続が不成立であることを検証
  - 方向が互換でない場合に接続が不成立であることを検証
  - dirty flagが配置/撤去でセットされ、再構築後にクリアされることを検証
  - 配置/撤去が発生していない場合に接続関係が変化しないことを検証
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 6.1, 6.2, 6.4_

- [x] 3.4 実装: 接続解決とdirty再構築
  - dirty-flagベースの遅延接続再構築を実装
  - ポートのワールド位置+方向から隣接ベルト位置を計算し、ベルトグリッドで存在チェックを行う接続解決ロジックを実装
  - 出力ポート: ベルト存在のみチェック。入力ポート: ベルト存在+方向互換チェック
  - ベルト配置/撤去時もdirty flagをセットする仕組みを実装
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 6.1, 6.2, 6.4_

- [x] 4. MachinePortTransfer — アイテム転送ロジック
- [x] 4.1 テスト: 出力ポートからベルトへの転送
  - 出力ポートにアイテムあり・接続先ベルト空 → 転送成功・ポートバッファクリアを検証
  - 出力ポートにアイテムあり・接続先ベルト満杯 → 転送スキップ・ポートバッファ維持を検証
  - 出力ポートにアイテムあり・接続先なし → 転送スキップ・ポートバッファ維持を検証
  - 出力ポートのバッファが最大1アイテムであることを検証
  - 転送前後のアイテム総数が保存されることを検証（出力ポート-1、ベルト+1）
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 5.2, 7.1, 7.3_

- [x] 4.2 実装: 出力ポート転送
  - 全出力ポートを走査し、バッファにアイテムがあり接続先ベルトが空なら転送する処理を実装
  - アトミック転送（ポートから除去→ベルトに追加）を保証
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 5.2, 7.1, 7.3_

- [x] 4.3 テスト: ベルトから入力ポートへの引き込み
  - 入力ポート空・接続先ベルトにアイテム到達済み → 引き込み成功を検証
  - 入力ポート満杯 → 引き込みスキップを検証
  - 入力ポートに接続先なし → 引き込みスキップを検証
  - 入力ポートのバッファが最大1アイテムであることを検証
  - 転送前後のアイテム総数が保存されることを検証（ベルト-1、入力ポート+1）
  - _Requirements: 4.1, 4.2, 4.3, 4.4, 5.1, 7.2, 7.3_

- [x] 4.4 実装: 入力ポート引き込み
  - 全入力ポートを走査し、バッファが空で接続先ベルトに到達済みアイテムがあれば引き込む処理を実装
  - アトミック転送（ベルトから除去→ポートに追加）を保証
  - _Requirements: 4.1, 4.2, 4.3, 4.4, 5.1, 7.2, 7.3_

- [x] 4.5 テスト: バックプレッシャー解消と保存則の統合検証
  - 出力先ベルト満杯→空き発生→転送自動再開を検証
  - 入力ポート満杯→アイテム消費→引き込み自動再開を検証
  - バックプレッシャー発生中のアイテム消失・重複なしを検証
  - 複数ティックにわたる出力→入力の連続転送でアイテム総数が保存されることを検証
  - いかなる状態でもアイテムの消失・重複が発生しないことを検証
  - _Requirements: 5.3, 5.4, 5.5, 7.1, 7.2, 7.3, 7.4_

- [x] 5. MachinePortSystemNode — システムアダプター統合
- [x] 5.1 テスト: ティック処理と配置/撤去イベントブリッジ
  - tick_firedシグナルで出力転送→入力転送が正しい順序で呼ばれることを検証
  - entity_placedシグナルで機械が正しくポートグリッドに登録されることを検証
  - entity_removedシグナルで機械が正しくポートグリッドから解除されることを検証
  - 非機械エンティティ（ベルト）の配置でもdirty flagがセットされることを検証
  - 非機械エンティティの配置でポート登録が行われないことを検証
  - _Requirements: 6.1, 6.2, 6.3, 6.4_

- [x] 5.2 実装: MachinePortSystemNode アダプター
  - TickEngineNodeのtick_firedシグナルを受信し、接続再構築→出力転送→入力転送を呼び出す薄いNodeアダプターを実装
  - PlacementSystemのentity_placed/entity_removedシグナルを受信し、機械タイプならポートグリッドに登録/解除するブリッジを実装
  - ベルトの配置/撤去時にもポートグリッドのdirty flagをセットする処理を実装
  - コアロジック（MachinePortGrid、MachinePortTransfer、MachinePortCatalog、BeltGrid）への依存注入を設定
  - _Requirements: 6.1, 6.2, 6.3, 6.4_

- [x] 5.3 Screenshot checkpoint: ティック処理と配置イベントの動作確認
  - ゲームシーンで機械とベルトを配置し、ポート登録・接続解決が正常に動作していることをログ出力で確認
  - _Requirements: 6.1, 6.2_

- [x] 6. E2E統合テスト
- [x] 6.1 E2Eフロー検証: 出力ポート→ベルト5本→入力ポートのアイテム到達
  - 出力ポート付き機械、ベルト5本、入力ポート付き機械を直線配置し、出力ポートにアイテムを投入して入力ポートに到達することを自動テストで検証
  - 投入アイテム数と受取アイテム数が完全一致することを検証（100アイテム投入→100アイテム受取）
  - _Requirements: 8.1, 8.2_

- [x] 6.2 全4方向回転でのポート転送検証
  - 出力ポート付き機械を4方向（N/E/S/W）それぞれに回転配置し、すべての向きでベルトへの転送が正常に動作することを検証
  - _Requirements: 8.3_

- [ ] 7. Human review: ポート位置の直感性
  - ゲーム実行中に各MVP機械（採掘機・精錬機・納品箱）を配置し、ポートの入出力位置がプレイヤーにとって直感的に理解できるかを目視確認
  - ポートの位置と方向がアートワーク・UIで視覚的に示唆されているか評価
  - 受け入れ閾値: 初見でも機械のどの辺にベルトを配置すればよいか迷わないこと
  - _Requirements: 1.1, 1.2_
