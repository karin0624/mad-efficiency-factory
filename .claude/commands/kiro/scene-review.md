---
description: L4 Human Review - Screenshot-based human review for Layer 4 tasks in tasks.md. Used as fallback when L3 E2E auto-evaluation is insufficient or uncertain.
allowed-tools: Read, Write, Edit, Glob, Grep, AskUserQuestion
argument-hint: <feature-name>
---

# シーンレビュー (L4 ヒューマンレビュー)

<background_information>
- **ミッション**: tasks.md の未完了のL4ヒューマンレビューサブタスクを処理し、レビュー基準を提示し、人間の判断を収集し、承認されたタスクを完了としてマークする。L3 E2EテストのAI評価が不確実または失敗した場合のフォールバックとしても機能する。
- **成功基準**:
  - tasks.md の保留中のL4ヒューマンレビューサブタスクをすべて特定する
  - レビュー基準と確認ステップをユーザーに提示する
  - E2Eテストで保存されたスクショがある場合は自動読み込みして提示する
  - 各項目について人間の合格/不合格の判断を収集する
  - 合格したタスクを tasks.md で `[x]` としてマークする
  - 不合格のタスクを実行可能なフィードバックとともに報告する
</background_information>

<instructions>
## コアタスク
フィーチャー **$1** の tasks.md からヒューマンレビューサブタスクを処理する。

## 実行ステップ

### ステップ 1: コンテキストの読み込み

**必要なコンテキストをすべて読み込む**:
- `.kiro/specs/$1/spec.json` — 言語とメタデータ
- `.kiro/specs/$1/tasks.md` — 保留中のヒューマンレビューサブタスク
- `.kiro/specs/$1/requirements.md` — レイヤー2/3のレビュー基準
- `.kiro/specs/$1/design.md` — ビジュアルデザイン仕様
- **`.kiro/steering/` ディレクトリ全体** — プロジェクトコンテキスト

### ステップ 2: 保留中のL4ヒューマンレビュータスクの抽出

**tasks.md を解析**して未チェックのL4ヒューマンレビューサブタスクを抽出する:
- パターン `- [ ] X.Y Human review: [criteria]` にマッチさせる
- タスク番号、レビュー基準、関連する要件IDを収集する
- L4ヒューマンレビュータスクが見つからない場合は報告して終了する

### ステップ 2.5: スクショの自動読み込み（E2Eテスト保存済みスクショの活用）

**E2Eテストで保存されたスクショの確認**:
- `godot/test_screenshots/` ディレクトリを確認し、利用可能なスクショファイルを一覧表示する
- 各レビュータスクに関連するスクショが存在する場合、Readツールで自動読み込みしてユーザーに提示する
- これによりユーザーはゲームを手動実行せずに視覚的な評価が可能になる
- スクショが存在しない場合は、手動確認手順をユーザーに提示する

### ステップ 3: レビュー項目の提示

保留中の各L4ヒューマンレビュータスクについて、ユーザーに以下を提示する:
- **タスクIDと説明**（tasks.md から）
- **レビュー基準**（タスクの説明と requirements.md の非テスト可能な側面から）
- **評価対象**: 具体的なビジュアル/感触/ユーザビリティの基準
- **Godot起動コマンド**: プロジェクトパスを絶対パスで記載（例: `godot --path /home/work/private/mad-efficiency-factory/godot/`）。worktreeで実行されている場合はworktreeの絶対パスを使用する。ユーザーはworktree外にいる前提で記載すること
- **操作手順**: タスク内容とdesign.mdから推測した具体的な操作ステップを番号付きで記載（例: 「1. ゲームを起動 → 2. マウスでグリッド上にホバー → 3. 左クリックで配置 → 4. ゴーストの半透明表示を確認」）
- **確認ポイント**: 何を見て合否を判断するか（タスクの `Human review:` 基準を具体的なチェック項目に展開）
- **判断の依頼**: 合格または不合格（任意でフィードバックを追加）

AskUserQuestion を使用して各項目についてユーザーの合格/不合格の判断を収集する。

### ステップ 4: tasks.md の更新

**合格した項目**:
- tasks.md でチェックボックスを `- [ ]` から `- [x]` に更新する

**不合格の項目**:
- チェックボックスは `- [ ]` のまま維持する
- ユーザーのフィードバックを実行可能な項目として記録する
- 再作業が必要なタスクと必要な変更内容を報告する

### ステップ 5: サマリー

すべてのレビュー結果のサマリーを提供する。

## 重要な制約
- **tasks.md 駆動**: tasks.md に未チェックのヒューマンレビュー項目として存在するタスクのみを処理する
- **人間中心**: このコマンドは自動テストではなく、人間の判断を支援するものである
- **反復的**: ビジュアルポリッシュサイクル中に複数回実行されることを想定している
- **自動合格なし**: 明示的なユーザー承認なしにヒューマンレビュータスクを完了としてマークしないこと
</instructions>

## ツールガイダンス
- **Read でコンテキスト取得**: レビュー基準のためにSpec要件を読み込む
- **Edit で更新**: Edit ツールを使用して tasks.md のタスクチェックボックスを更新する
- **インタラクティブ**: 各レビュー項目について AskUserQuestion を使用して合格/不合格を収集する

## 出力説明

spec.json で指定された言語で出力する:

1. **保留中のレビュー**: tasks.md で見つかったヒューマンレビュータスクの一覧
2. **レビュー基準**: 各項目についてユーザーが確認すべき内容
3. **レビュー結果**: 各項目の合格/不合格とユーザーフィードバック
4. **更新されたタスク**: tasks.md で完了としてマークされたタスク
5. **残作業**: 不合格の項目と実行可能な次のステップ
6. **次のアクション**: すべて合格の場合はフィーチャーレビュー完了。不合格がある場合は `/kiro:spec-impl` で修正後、`/kiro:scene-review` を再実行

**フォーマット**: 簡潔に。テキストは300語以内。

## 安全策とフォールバック

### エラーシナリオ

**ヒューマンレビュータスクが見つからない場合**:
- tasks.md に保留中のヒューマンレビューサブタスクがないことを報告する
- 提案: 「すべてのヒューマンレビュータスクが既に完了しているか、レイヤー3分類でタスクが生成されていない可能性があります」

**Specファイルが見つからない場合**:
- 処理を停止し、先行フェーズを完了するよう案内する

### 統合

**ワークフロー上の位置**: `/kiro:spec-impl` 完了後に実行する（spec-impl はL4ヒューマンレビュータスクをスキップし、L3 E2Eタスクは自動実行する）
- `/kiro:spec-impl $1` - 自動実行可能なすべてのタスク（L1/L2/L3）を実行し、L4ヒューマンレビューをスキップ
- `/kiro:scene-review $1` - スキップされたL4ヒューマンレビュータスクを拾い上げ、人間の承認を得る（E2Eスクショの自動読み込み付き）
- ビジュアルポリッシュ中に必要に応じて繰り返す
